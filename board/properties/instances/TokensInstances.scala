package board 

import cats.Monoid
import util.Random

object TokensInstances {
  
  // // not useful placeholder.
  // val tokensMonoid1: Monoid[Tokens] = new Monoid[Tokens] {
  //   def combine(x: Tokens, y: Tokens) = x
  //   def empty = Tokens(Nil)
  // } 

//   val m = Seq(
//     tokensMonoid1,
//   )
//   implicit val tokensMonoid = m(Random.nextInt(m.size))
// }

}
